name: Publish Packages to GitHub Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dist_tag:
        description: 'Dist-tag (e.g. latest, beta, next, v0.5.0)'
        required: true
        default: 'latest'

jobs:
  publish:
    name: Publish Packages to GitHub Packages (npm.pkg.github.com)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://npm.pkg.github.com'
          scope: '@bradtech'

      - name: Install dependencies & build
        run: |
          bun install --frozen-lockfile
          bun run build
          bun test

      - name: Publish packages to GitHub Packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BUN_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event.inputs.dist_tag || 'latest' }}"
          echo "Publishing packages to GitHub Packages with dist-tag: $TAG"
          
          # Topological publication order: base types -> ontologies -> core sensor -> domain sensors -> lorawan
          PKGS=(
            "packages/types"
            "packages/ontologies"
            "packages/sensor"
            "packages/sensor-power"
            "packages/sensor-acoustic"
            "packages/sensor-air"
            "packages/sensor-soil"
            "packages/sensor-weather"
            "packages/sensor-lorawan"
          )
          
          for pkg in "${PKGS[@]}"; do
            if [ -d "$pkg" ] && [ -f "$pkg/package.json" ]; then
              IS_PRIVATE=$(node -p "require('./$pkg/package.json').private || false")
              if [ "$IS_PRIVATE" != "true" ]; then
                echo "🚀 Publishing $pkg to GitHub Packages..."
                (cd "$pkg" && npm publish --tag "$TAG" || echo "⚠️ Warning: failed publishing $pkg")
              fi
            fi
          done
