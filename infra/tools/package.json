{
  "name": "@bradtech-oss/cli-setup",
  "version": "0.5.0",
  "private": true,
  "description": "Interactive CLI Setup Generator for Secret Keys & Gateway Config Archives (Bun native CLI)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "brad-setup": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
